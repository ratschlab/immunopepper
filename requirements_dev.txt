pip>=9.0.1
bumpversion>=0.5.3
wheel>=0.30.0
watchdog>=0.8.3
flake8>=3.5.0
tox>=2.9.1
coverage>=4.5.1
Sphinx>=1.7.0
pytest>=3.4
pytest-runner>=2.11.1
prospector==0.12.8
vulture>=0.6
# TODO: this dependency is at the wrong place, should be in requirements.txt.
# However, then we get into issues with make install and tox. Will be fixed once
# spladder is in pypi
git+https://github.com/ratschlab/spladder.git@feature/simpleDeployment#subdirectory=python&egg=spladder-1.1.0
